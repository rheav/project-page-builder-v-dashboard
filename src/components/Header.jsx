const Header = ({ sectionTitle }) => {
	return <header className="fixed top-0 left-0 bg-gradient-to-r from-startCold to-endCold w-full py-3 px-8 text-center text-white text-3xl font-bold tracking-wide lowercase">{sectionTitle}</header>;
};

export default Header;
